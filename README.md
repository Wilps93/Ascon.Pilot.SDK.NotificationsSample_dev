# TOMS.moduls - Модуль расширения для Ascon.Pilot.SDK

## 📋 Описание

Модуль расширения для системы Ascon.Pilot, обеспечивающий автоматическую обработку уведомлений, интеграцию с Rocket.Chat и отправку email-уведомлений.

## 🏗️ Архитектура

### Основные компоненты:

- **Configuration** - Безопасная система конфигурации
- **Services** - Сервисы для работы с уведомлениями, чатом и email
- **Validation** - Валидация входных данных
- **Logging** - Централизованная система логирования

### Структура проекта:

```
├── Configuration/
│   └── AppSettings.cs          # Безопасная конфигурация
├── Services/
│   ├── LoggingService.cs       # Система логирования
│   ├── NotificationService.cs  # Обработка уведомлений
│   ├── ChatService.cs          # Интеграция с Rocket.Chat
│   └── EmailService.cs         # Отправка email
├── Validation/
│   └── InputValidator.cs       # Валидация данных
├── Main.cs                     # Основной класс модуля
└── App.config                  # Конфигурационный файл
```

## 🔧 Установка и настройка

### Требования:
- .NET Framework 4.8.1
- Ascon.Pilot.SDK
- Newtonsoft.Json 13.0.0+

### Настройка конфигурации:

1. Отредактируйте файл `App.config`
2. Настройте параметры Rocket.Chat и SMTP
3. При необходимости зашифруйте чувствительные данные

```xml
<appSettings>
  <!-- Rocket.Chat настройки -->
  <add key="RocketChat:BaseUrl" value="http://your-server:3000" />
  <add key="RocketChat:AuthToken" value="your-token" />
  <add key="RocketChat:UserId" value="your-user-id" />
  
  <!-- SMTP настройки -->
  <add key="Smtp:Host" value="mail.your-domain.com" />
  <add key="Smtp:Port" value="587" />
  <add key="Smtp:Username" value="your-email@domain.com" />
  <add key="Smtp:Password" value="your-password" />
</appSettings>
```

## 🚀 Использование

### Основные функции:

1. **Автоматическая обработка уведомлений**
   - Создание объектов
   - Изменение атрибутов
   - Изменение подписей
   - Удаление объектов

2. **Интеграция с Rocket.Chat**
   - Отправка уведомлений в чат
   - Создание приватных комнат
   - Обработка сообщений

3. **Email уведомления**
   - HTML-шаблоны
   - Уведомления о заданиях
   - Уведомления об изменениях состояния

### Пример использования:

```csharp
// Создание сервиса уведомлений
var notificationService = new NotificationService(repository, modifier);

// Обработка уведомления
await notificationService.ProcessNotificationAsync(notification);
```

## 🔒 Безопасность

### Реализованные меры безопасности:

1. **Шифрование конфигурации**
   - AES-шифрование чувствительных данных
   - Безопасное хранение паролей и токенов

2. **Валидация входных данных**
   - Проверка email адресов
   - Валидация URL
   - Проверка GUID
   - Защита от XSS

3. **Логирование безопасности**
   - Детальное логирование операций
   - Отслеживание ошибок
   - Ротация логов

## 📊 Логирование

### Уровни логирования:
- **Debug** - Отладочная информация
- **Info** - Информационные сообщения
- **Warning** - Предупреждения
- **Error** - Ошибки
- **Critical** - Критические ошибки

### Расположение логов:
```
logs/
├── pilot-module.log          # Текущий лог
├── pilot-module.20250101_120000.log  # Архивные логи
└── ...
```

## ⚡ Производительность

### Оптимизации:

1. **Асинхронная обработка**
   - Все I/O операции асинхронные
   - Неблокирующие HTTP-запросы

2. **Кэширование**
   - Кэш пользователей
   - Кэш настроек

3. **Ограничения**
   - Максимум 10 одновременных запросов
   - Таймаут 30 секунд на запрос
   - Ограничение размера сообщений

## 🧪 Тестирование

### Рекомендуемые тесты:

1. **Unit тесты**
   - Валидация данных
   - Обработка уведомлений
   - Работа с конфигурацией

2. **Интеграционные тесты**
   - Подключение к Rocket.Chat
   - Отправка email
   - Обработка реальных уведомлений

3. **Нагрузочные тесты**
   - Множественные уведомления
   - Большие объемы данных

## 🐛 Отладка

### Полезные команды:

```csharp
// Проверка конфигурации
var configResult = InputValidator.ValidateConfiguration();
LoggingService.LogInfo(InputValidator.GetValidationSummary(configResult));

// Тест подключения к Rocket.Chat
var chatService = new ChatService();
var isConnected = await chatService.TestConnectionAsync();

// Тест SMTP
var emailService = new EmailService();
var smtpOk = await emailService.TestConnectionAsync();
```

## 📈 Мониторинг

### Метрики для отслеживания:

1. **Производительность**
   - Время обработки уведомлений
   - Количество ошибок
   - Размер логов

2. **Безопасность**
   - Попытки доступа
   - Ошибки валидации
   - Критические ошибки

3. **Интеграции**
   - Статус Rocket.Chat
   - Статус SMTP
   - Успешность отправки

## 🔄 Версионирование

### Текущая версия: 10.7.25.0

### История изменений:

#### v10.7.25.0 (2025-01-01)
- ✅ Рефакторинг архитектуры
- ✅ Безопасная конфигурация
- ✅ Централизованное логирование
- ✅ Валидация входных данных
- ✅ Асинхронная обработка
- ✅ HTML-шаблоны для email

## 📞 Поддержка

### Контакты:
- **Компания:** НИИПИ "ТОМС"
- **Продукт:** TOMS.moduls
- **Версия:** 10.7.25.0

### Логи и отладка:
- Логи находятся в папке `logs/`
- Уровень логирования настраивается в `App.config`
- Критические ошибки показываются в MessageBox

## 📄 Лицензия

Copyright © 2025 НИИПИ "ТОМС". Все права защищены.

---

**Примечание:** Этот модуль является частью системы Ascon.Pilot и требует соответствующей лицензии для использования.